"use client";

export default function SearchResultsPage({
  params,
}: {
  params: { term: string };
}) {
  return (
    <div className="bg-zinc-900 text-white pt-12 h-full">
      Search results page
    </div>
  );
}
